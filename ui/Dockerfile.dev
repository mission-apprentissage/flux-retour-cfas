FROM node:16.6.0-stretch

ARG REACT_APP_ENV
ARG REACT_APP_CRISP_WEBSITE_ID

ENV REACT_APP_ENV=$REACT_APP_ENV
ENV REACT_APP_CRISP_WEBSITE_ID=$REACT_APP_CRISP_WEBSITE_ID

WORKDIR /app

COPY package.json yarn.lock ./

RUN yarn install --frozen-lockfile

COPY ./ /app

EXPOSE 3000
CMD yarn start
