name: CI
on: [ push ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2

      - run: make ci
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
